# 流萤 Fairy · 交接说明

> 本文件是**换人接手时第一份要读的文档**。写于 2026-09-12，对应版本 **v0.5.5**。
> 新接手的同学：先读完这一份，再动代码。里面有架构铁律、踩过的坑、验证流程和待办清单。

---

## 0. 一句话：这是什么

`D:\流萤\fairy` 是一个 **本地优先的中文语音陪伴助手**（Python 单机应用，Windows）。

- 名字「流萤 / Fairy」，定位是**情感陪伴**（不是效率工具），会说话、有情绪、记得住事。
- 语音能力（ASR 识别 / TTS 合成 / LLM 大脑）全部走 **小米 MiMo** 的 OpenAI 兼容接口。
- 有图形控制台（tkinter 六页）、桌宠、记忆库（SQLite）、情绪模型。
- 可按需调用外部 agent **Pi**（编程智能体）干活，但**平时与 Pi 零耦合**。

**当前用户**：中文沟通，居住中国，Windows 环境。

---

## 1. 当前状态快照（2026-09-12）

| 项 | 状态 |
|---|---|
| 版本 | **v0.5.5** |
| 离线自检 | **42/42 全绿**（`python main.py --selftest`） |
| 打包后自检 | `dist\流萤.exe --selftest` **42/42**，exit=0 |
| 工作区 | 干净（`git status` 无输出） |
| 最近提交 | `366b567 fix: Pi 超时不再留孤儿进程 + 500 说人话（v0.5.5）` |
| 产物 | `dist\流萤.exe`（33.1MB，14:08）= **v0.5.5 当前版** |
| `dist` 备份 | `流萤.上一版.v0.5.4.exe`、`流萤.上一版.v0.5.4.bak.exe`（可删） |

⚠️ **`dist\config.json` 含 API Key，外发前必须清空。**

### 最近几个版本做了什么

| 版本 | commit | 内容 |
|---|---|---|
| v0.5.5 | `366b567` | **修 Pi 超时孤儿进程**（`_kill_tree` 用 taskkill /T /F）+ 500 友好提示 + speaker join |
| v0.5.4 | `534b1b9` | **D1 配置热重载**（改音色不用重启）+ GUI 可见性回调 |
| v0.5.3 | `73cefff` | D3 合成方式联动字段、D7 情绪开关提示、全面补齐确认与开关 |
| — | `a66c371` | 模块联动 P1+P2（情绪注入上下文 / 召回重排 / 上下文可视化） |

---

## 2. 项目结构（主要文件）

```
fairy.py            唯一入口（双击=图形控制台，--pet=桌宠，--text=键盘对话，
                    --selftest=自检，api <子命令>=对外 JSON 接口）
main.py             Fairy 主类：对话、语音、动作、桌宠、统计（注意：是"上帝类"）
gui.py              tkinter 图形控制台，六页
core/
  config.py         配置加载；config_path() / resolve_root()（项目根唯一来源）
  llm.py            大脑（OpenAI 兼容）；chat / chat_stream
  tts.py            语音合成（MiMo）；tts_field_states 等纯校验函数
  asr.py            语音识别
  memory.py         SQLite 记忆库（FTS5 全文检索）
  emotion.py        情绪模型（三维数值 + 风格指令）
  agent_backend.py  外部 agent 注册表（Pi 后端在这里）
  actions.py        动作执行（读文件/列目录/删文件/get_time…）
  safety.py         危险动作两级闸口
  pet.py            桌宠
  http.py           requests.Session 单例（连接池复用）
  sentence_buffer.py 流式切句
  winconsole.py     隐藏黑窗口
fairy_api.py        对外单行 JSON CLI（入站扩展点）
tests/smoke_test.py 离线自检（42 项）
build_exe.py        PyInstaller 打包
persona/            人设 md
data/               memory.db / audit.log / stats.json
dist/               打包产物
```

---

## 3. 架构铁律（**不可偏离**）

1. **陪伴端是主体**，自身的 SQLite 是**唯一**长期记忆存储。
2. **Pi 是"按需外包"**：只有用户**明确要求**（`/pi <任务>`）时才以**子进程**调用 `pi` CLI。
   调用方向永远是 **Python → Pi**。
3. **绝不共享记忆**：Pi 不读不写陪伴端的记忆库；Pi 的结果作为"笔记"回流到陪伴端记忆。
4. **两条扩展线必须保留**：
   - 出站：`core/agent_backend.py` 的注册表（换 agent 只写一个类 + `@register_backend`）
   - 入站：`fairy_api.py` 单行 JSON CLI（外部宿主调用陪伴端能力的唯一入口）

---

## 4. 改代码前必读的坑（按模块）

### 情绪模型（`core/emotion.py`）
- 三维数值：valence[-1,1] / arousal[0,1] / intimacy[0,1]，存 `emotion_state` + `emotion_log`。
- **情绪有两个出口，别混**：
  - `style_instruction()` / `_guidance()` → **怎么念**（语速/气息/尾音）→ 发给 **TTS**
  - `context_line()` / `_mood_guidance()` → **说什么**（措辞/态度）→ 拼进 **system prompt**
  - 只做前者会出现"语气低沉但措辞活泼"的人格分裂。开关 `emotion.inject_to_context`（默认 true）
- **情绪实例必须全局唯一**：由 `gui.ChatWorker` 持有一份，经 `Fairy(cfg, emotion=...)` 注入。
  `Fairy._owns_emotion` 记录是不是自己造的。**新增情绪消费者时别再造第二份。**
- MiMo 官方方案：自然语言指令放 `role=user`，要念的正文放 `role=assistant`。
- 提示词必须写明**共情方向**（用户低落时流萤要跟着低落），且要过滤"安慰/建议"这类动作词，
  否则会反共情（已实测踩坑）。

### 上下文装配（`main.py::_prepare_messages`）
- 顺序**固定**：先 `build_recall_block()` 召回 → 再 `memory.add(用户这句)` → 再 `recent()` 取近 N 轮。
  顺序反了会让用户当前这句话**必然被自己召回**（模型看到"你刚提到 X"式自我复读）。
  `recent()` 必须在 add 之后，才能看到本轮。
- system prompt 区块顺序：**人设 → 动作说明 → 往事召回 → 此刻心情**。
- ⚠️ `fairy_api.py` 的 `_system_block()` 是**同一规则的第二处实现**，改一处必须同步另一处。
- 情绪预判 `pre_turn_hint()` 必须在拼 system prompt **之前**调用，否则本轮注入的是上一轮心情。

### 记忆与召回（`core/memory.py`）
- 排序：`score = 重要度×0.35 + 分类加分 + 相关度(-bm25)`，候选池取 `limit*3` 再重排。
  ⚠️ **SQLite 的 `bm25()` 返回负值**，要取负才是"越大越相关"。
- 重要记忆常驻：`pinned()` 把重要度 ≥ 8 且未过期的无条件注入（上限 5 条），
  单列「【我一直记得的重要事情】」。开关 `memory.pin_important`。
- 长问句拆词兜底：`_core_terms()` 去停用词后取 2~3 字窗口逐个 LIKE。
  ⚠️ 停用词**必须按长度倒序替换**（否则「怎么样」被「怎么」吃半截）；
  **核心词被吃成单字时要退回原句切片**。

### 配置热重载（v0.5.4）
- 每轮对话前 `Fairy.reload_config_if_changed()` 探一次。
- **指纹用文件内容 sha1，不要用 mtime**：同秒改写、或文件大小不变（「冰糖」→「苏打」等长）会漏判。
- 配置文件位置统一走 `core/config.py::config_path()`，**别在别处重算**。
- **情绪实例绝不重建**：走 `EmotionModel.apply_config()`，只刷新配置驱动的属性。
  亲密度是长期聊出来的，重建或 reset 都会抹掉。
- 报 API Key 时**只说"已更新"、绝不回显内容**。
- **测试坑**：验证热重载时不能拿传给 Fairy 的那个 dict 当"新配置"去改（`f.cfg` 会跟着一起变，
  新旧比对永远"没差异"）。必须 deepcopy 出一份新对象再写盘。

### Pi 后端（`core/agent_backend.py`）
- 本机 Pi v0.85.1，路径 `D:\Pi\pi.CMD`。**改 Pi 相关代码前先跑 `pi --help` 核对参数。**
- **不存在** `--print-turn`、`--no-markdown`（旧文档里有，本机报 Unknown options）。
- Windows 上 pi 是 `pi.cmd`，需 `cmd /c` 包裹。
- ⚠️ **超时杀进程必须杀整棵树**（v0.5.5 修）：`proc.kill()` 只杀 `cmd.exe` 外壳，
  真正的 node 子进程会变孤儿继续占资源，多次超时后拖垮后续大脑/语音请求。
  必须走 `_kill_tree()`（`taskkill /T /F /PID` + `proc.wait` 回收）。

### GUI 交互约定（v0.5.3 起）
- **加确认的判据**：只看一条——**这个操作会不会造成不可逆的损失**。会，就必须确认；
  "改一下还能改回来"的一律不加。已加：清理过期记忆、情绪重置、换 API Key、退出时保护未保存。
  **故意不加**：重要度 ±1、情绪微调、试听/体检/导出/分页。
- **加开关的判据**：**是我加的行为，就得给用户关掉它的开关**。
- ⚠️ **写盘路径不止一条**：`_save_config` 与 `_save_voice_settings` 都会写 config.json，
  **两条都必须刷新 `_cfg_snapshot`**，否则退出时会误报"没保存"。
- 控制台是**六页**：对话 / 记忆 / 情感 / 配置 / 统计 / 状态。
- `gui.py` 用模块级 `APP_ROOT = resolve_root()`；**不要用 `core.config.ROOT`**。

### MiMo TTS 官方规范（已踩坑核实）
- 三模型：`mimo-v2.5-tts`（预置音色）/ `-voicedesign`（文本设计）/ `-voiceclone`（音频复刻）。
- ⚠️ **voiceclone 的参考音频必须放 `audio.voice` 字段**（`data:audio/wav;base64,...`），
  **不要**用顶层 `reference_audio`（那是第三方代理写法，官方会忽略 → 克隆失效）。
- voicedesign **不传** voice 参数；正文永远 role=assistant，指令/描述放 role=user。
- 预置音色：mimo_default / 冰糖 / 茉莉 / 苏打 / 白桦 / Mia / Chloe / Milo / Dean。
- 校验纯函数都在 `core/tts.py`（`voice_choices` / `validate_reference_audio` / `tts_settings_issues`），
  GUI 与自检共用；改 TTS 逻辑要同步改这三处。

---

## 5. 验证体系

### 离线自检（**必须 42/42 全绿**）
```bash
python main.py --selftest
```
- 必须**离线**：关掉任何会联网的推断（`emotion.infer_with_llm=False`），`stats.path` 指临时目录。
- 必须**打包兼容**：不能 spawn `python xxx.py`（打包后没有 python），
  也不能拿 `__file__` 推断项目根（打包后指向临时目录）。

### 隔离 GUI 冒烟（改 GUI 后必做）
按技能 `tkinter-gui-smoke-test`（已装在 `~/.workbuddy/skills/`）的做法：
```
FAIRY_ROOT 指临时目录 → sys.path.insert(0, PROJECT) → import gui
→ 把 gui.messagebox 换成假对象、按序返回预设答案
→ 直接调 app._xxx() 方法 → 用 widget.cget("state") 断言
```
- `load_config` 会把 db_path/audit_log/persona_path 解析到 FAIRY_ROOT 下，不污染真实数据。
- ⚠️ `ChatWorker` 的 fairy **只在处理 job 时才创建**，探针要先 `_ensure_fairy(load_config())`。
- ⚠️ `chat_box` 是 ScrolledText 复合控件，直接 `app.chat_box.get()`，别扫子控件。

### 改完源码的标准流程
```
py_compile → 开发环境 --selftest → 改名旧 exe → build_exe.py
→ 比对 PYZ mtime → dist\流萤.exe --selftest
```

---

## 6. 打包与发布（PyInstaller）

- `build_exe.py` → `dist\流萤.exe`（**单文件**，控制台子系统，**不加 `--noconsole`**）。
- 打包参数必需：`--hidden-import tests.smoke_test`、`--hidden-import pystray._win32`、
  `--collect-all sounddevice`、`--add-data persona`、`--add-data config.example.json`。
- ⚠️ **打包铁律**：PyInstaller 下 `__file__` 指向临时解包目录 `_MEIPASS`。
  一切"项目根"必须走 `core/config.py::resolve_root()`，
  **禁止再用 `Path(__file__).parent.parent` 当项目根**。
- ⚠️ **重新打包前必须先把旧 `dist\流萤.exe` 改名挪开**：
  本机沙箱会拦截 PyInstaller 覆盖旧 exe 的 `os.remove` 并 fail-closed。改名不受拦截。
- ⚠️ **千万别在打包进行中改源码**：PyInstaller 在 PYZ 阶段（约占整轮前 3/4 时间）就把 .py
  固化成字节码。中途改源码照样"打包成功"，但**内容不含该改动**。
  判别：比对源码 mtime 与 `build\流萤\PYZ-00.pyz` 的 mtime（PYZ 必须更新）。
- 提交后 `.git/hooks/post-commit` 会自动往 CHANGELOG.md 的「Git 提交记录」表追加一行。
  钩子在 `.git/hooks/` 属本地文件，**不入版本库**，换机器克隆后需重新安装。

### 本机沙箱注意事项
- `Remove-Item` 会报 `SAFE_DELETE_FAIL_CLOSED`，但**实测有时文件其实已删掉**（报错≠没删）。
  删完立刻 `Test-Path` 核对，别照着报警信息下结论。
- **中间产物改名挪开（同盘 rename）永远是最稳的绕过方式**。
- 用独立脚本验证本项目时，必须在 import 前 `sys.path.insert(0, str(PROJECT))`，
  否则 `ModuleNotFoundError: No module named 'gui'`。

---

## 7. 已知技术债 & 下一步建议

**技术债**（尚未处理，都记录在 `MEMORY.md` 与本报告）：
1. `Fairy` 仍是"上帝类"（陪伴+语音+动作+桌宠+统计），尚未拆分 —— **最大的一笔债**
2. 库内仍有 `print(..., flush=True)` 裸日志，未统一到 `logging`
3. 配置仍是全局 dict 驱动，未引入 dataclass
4. 桌宠只有 4 个动作状态，**尚未与情绪模型联动**（用户可能会想要这个）

**可选改进**（上一轮提过、用户未明确要求）：
- 把 LLM 失败的完整原始返回写进 `data/audit.log`，方便日后排查或找客服
  （注意：LLM 失败是高频事件，写日志会让 audit.log 膨胀，需权衡）

**审查报告 `模块联动审查报告_声音_情感_记忆.md` 里列出的缺口已全部关闭**
（P0 三项 + P1 四项 + P2 两项 + D1/D3/D7）。唯一保留的是"记忆不做全量注入"这个刻意取舍。

---

## 8. 环境与命令速查

```bash
# Python（优先项目内 venv）
D:\流萤\fairy\.venv\Scripts\python.exe

# 自检
python main.py --selftest

# 对话与体检
python main.py --text          # 键盘对话
python main.py --devices       # 音频设备列表
python main.py --mic-test      # 麦克风体检
python main.py --diag          # 云端服务体检（ASR/TTS/LLM 三项）
python main.py --emotion       # 看情绪状态与将发给 TTS 的指令
python main.py --stats         # 用量统计
python main.py --pi-check      # 检查 Pi 是否可用

# 打包
python build_exe.py            # 或双击 打包exe.bat

# 对外接口
fairy.exe api ping
fairy.exe api persona
```

**当前大脑配置**（`config.json`，已核实正常）：
- base_url：`https://token-plan-cn.xiaomimimo.com/v1`
- model：`mimo-v2.5`
- api_key：在 `mimo` 段（`tp-` 开头，Token Plan 格式，正常）

---

## 9. 用户偏好（重要）

- **中文沟通为主**，居住中国，Windows 环境。
- **用户自述对代码一窍不通**（纯非技术背景）：
  - 解释技术/错误概念时**用大白话**，避免堆砌专业术语
  - 给排查/操作指引时，写成**可逐句执行的编号步骤**
- 偏好**结构化输出**：编号清单、表格、"改了什么 + 验证结果 + 下一步"三段式。
- **每次代码改动后，期望先跑测试验证通过再报告完成**；只给理论解释不够。
- 交付时希望提供：变更列表 + 测试/验证结果 + git commit hash + 用户验证指引。
- 遇到"修复无效"时，会要求做**二进制式根因排查**（逐个环节验证，而不是猜）。

---

## 10. 接手后的第一件事

1. 跑 `python main.py --selftest` 确认 **42/42 全绿**（基线）
2. 读 `模块联动审查报告_声音_情感_记忆.md` 了解历史缺陷与修复脉络
3. 读 `CHANGELOG.md` 的 v0.5.3 ~ v0.5.5 了解最近三轮做了什么
4. 与用户确认下一步目标（是从技术债入手，还是继续加功能）
