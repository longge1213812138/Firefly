# CHANGELOG · Firefly（流萤）本地语音助手

本地优先的语音私人助手，基于 Python 实现（MVP 阶段），情感陪伴 + 电脑操作 + 记忆系统。语音能力全部走小米 MiMo（ASR / TTS / LLM）。

> 本文件的「Git 提交记录」表由 `.git/hooks/post-commit` 钩子自动维护，每次提交后自动追加一行，无需手动编辑。

---

## 版本记录

### v0.5.5（Pi 超时不再留孤儿进程，500 有话说人话）

**修掉"Pi 超时后情感陪伴也聊不动"的连锁故障**
- 根因：Pi 超时后 `proc.kill()` 只杀掉了 `cmd.exe` 外壳（Windows 上 pi 是 `pi.cmd`，
  被 `cmd /c` 包了一层），真正的 `node` 子进程变成**孤儿进程**继续跑、继续占
  CPU/内存/管道句柄。多次超时后孤儿进程越攒越多，把后续的大脑/语音请求拖垮，
  表现为"Pi 超时之后突然不能正常聊天了"。
- 修复：`core/agent_backend.py` 新增 `_kill_tree()`——Windows 下用系统自带
  `taskkill /T /F /PID` 把整棵进程树一起端掉并 `wait()` 回收，非 Windows 退回 kill；
  超时分支改走它。杀干净了，Pi 超时不再污染后续对话。

**让"大脑 500"不再是一句干巴巴的报错**
- `core/llm.py` 把常见 HTTP 错误的人话解释抽成 `_HTTP_HINTS`（chat / chat_stream 共用），
  新增 429 / 500 / 502 / 503：500 会明确说"服务端临时故障，稍后重试通常就好，
  多半不是你的配置问题"，用户不会再以为是自己程序坏了。
- 顺带修 `respond_stream` 流式失败回退非流式时，`speaker.cancel()` 后没有 join，
  可能残留播报线程/抢麦——补 `speaker.join(timeout=5)` 让线程退干净。

- 🧪 自检 **40 → 42 项**，42/42 全绿（新增「Pi超时杀进程树」「大脑错误友好提示」）

### v0.5.4（配置热重载：改音色不用重启了）

**D1 修掉"改了设置却没生效"的最后一处**
- 此前命令行（`--text`）与语音模式**启动时只读一次配置**：在控制台改完音色，
  用「启动助手.bat」那个窗口必须重启程序才生效，很多人会以为"设置没生效"
- 现在每轮对话前用 **config.json 的内容 sha1** 探一下（用内容不用 mtime ——
  同秒内改写、或文件大小没变时会漏判），变了就**就地热重载**：
  - 重建 TTS / ASR / 大脑 LLM（下一轮才用到，不会打断本轮）
  - 重新读人设文件
  - **情绪实例不重建**，只把配置项重读进去（`EmotionModel.apply_config()`）——
    亲密度是长期聊出来的，重建会把累积状态抹掉
  - 记忆库路径若被改过，顺带重开
  - 会打印到底改了什么，例如「（配置已热重载：音色 冰糖 → 苏打）」
- `--text` 模式新增 **`/reload`** 命令：想立刻生效不用等下一句；
  启动提示里也写明了"改了 config.json 不用重启"
- 顺带对齐：`core/config.py` 抽出 `config_path()`，配置文件位置只在一处解析——
  `load_config`、`gui.py` 的两处保存（保存配置 / 保存声音设置）与热重载全部走它，
  以后改配置文件名不会再漏改
- 🔒 热重载报告里 **API Key 只说"已更新"、绝不回显内容**

- 🧪 自检 **39 项 → 40 项**，40/40 全绿；新增用例覆盖"没改不重载 / 改了重建音色 /
  不重复重载 / 情绪设置跟着变但亲密度保留"以及"控制台 print + GUI 回调都生效"
- 🧪 按 `tkinter-gui-smoke-test` 技能做了隔离 GUI 冒烟（11 项断言全过）：六个页面都能建、
  保存配置后不会误判为需要热重载、**在程序外面改 config.json 能被自动接住**、
  热重载后情绪仍是同一实例、`on_config_reload` 真的被 ChatWorker 挂到 Firefly、
  `reload_note` 事件落到状态栏 + 聊天区 sys 行、退出保护照旧

**GUI 侧把热重载变得看得见**
- `Firefly.on_config_reload` 回调：Firefly 热重载时把变更笔记回交给上层
- `ChatWorker` 在 chat / voice_input 两条入口挂上回调 → 把变更塞进 ui 队列
- 控制台侧 `_poll_ui` 新增 `reload_note` 事件：状态栏立刻闪「⚙ 配置已热重载：xxx」、
  聊天区同时落一条 sys 行（事后也能回看，这次到底改了啥）
- 控制台模式：每轮对话结束 `_say_line` 打印一行「（配置已热重载：…）」
  ——空列表（即真没改动）则**不刷屏**，探针用 `redirect_stdout` 抓了 0 行输出做断言

### v0.5.3（交互打磨：该禁用的禁用，该确认的确认）

**按合成方式联动字段可用性（D3）**
- 此前「音色 / 音色描述 / 参考音频」三个框一直全开，voiceclone 下还能选音色，
  让人误以为"克隆要先选音色"。现在按合成方式实时启用/禁用 + 改标签：
  | 合成方式 | 音色 | 中间那一栏 | 参考音频 |
  |---|---|---|---|
  | 预置音色 | ✅ 可编辑 | 「风格指令」（可选） | ⛔ 禁用 |
  | 音色设计 | ⛔ 禁用 | 「音色描述」（必填） | ⛔ 禁用 |
  | 声音克隆 | ⛔ 禁用 | 「风格指令」（可选） | ✅ 必填，可浏览/录制 |
- 判断规则抽成纯函数 `core/tts.py::tts_field_states()`，界面与自检共用一套说法；
  认不出的新模型一律全开，不把用户手脚捆住

**情绪开关的生效时机说清楚（D7）**
- 配置页「情绪模型」分组新增实时生效提示：勾上/取消分别会怎样、**点「保存配置」即刻生效**
- 情感页新增状态行：区分「已启用（微调与配置改动即刻生效）」与
  「已关闭——语音与文字都不再带情绪」，并加「⚙ 去配置页改情绪设置」一键跳转

**不可逆操作补齐确认（自查后新增）**
- **「🧹 清理过期」**：原来**直接删、无任何确认**。现在先查数量 → 报"将永久删除 N 条" →
  二次确认；没有过期数据时只提示不删（并说明普通对话默认永不过期）。统计页同名按钮同样受益
- **「🔁 重置到基准」**：会连**累积的亲密度**一起重置且无法撤销，现在弹窗列出
  「当前值 → 基准值」三维对照再确认
- **更换 API Key**：旧 Key 出于安全**从不回显**，覆盖后找不回来。现在换 Key 会先确认，
  选「否」= 不换 Key 但**其余设置照常保存**（并在结果里注明"API Key 未更换"）

**未保存修改不再悄悄丢（自查后新增）**
- 配置页底部新增「● 有 N 项修改未保存」提示（切页面时刷新）
- **退出程序时若有未保存的配置/人设改动会拦截并列出具体字段**，避免"改了却没生效"
- 实现上只登记界面变量、不做配置键映射，避免和 `_save_config` 各写一份映射日久走样；
  「保存声音设置」这条**独立写盘路径**也已同步刷新快照（否则退出会误报）

**补一个缺失的开关（自查后新增）**
- P1-6 的「回复前用关键词预判情绪」此前是**写死的**，现在给了开关 `emotion.pre_hint`（默认开），
  配置页可勾选——我加的行为都应该能被关掉

**其他打磨**
- 「🔍 本轮上下文」窗口改为**单例**，反复点不再开出一堆一样的窗口

- 🧪 自检 **36 项 → 38 项**（新增：合成方式联动字段可用性、过期记忆计数与清理），38/38 全绿；
  另做了覆盖 22 项断言的隔离 GUI 冒烟（字段联动 / 提示文案 / 四个弹窗的取消与确认分支 /
  两条写盘路径的脏标记 / 人设脏检测）

### v0.5.2（模块联动 P1 + P2：把「注入」做真）

- ✅ **P1-4 情绪真正注入大模型上下文**（此前只影响语音、不影响措辞）：`_system_prompt()` 新增一段
  约 60~110 字的「此刻的心情」状态行，由 `core/emotion.py` 的 `context_line()` 生成。
  与原来的 `style_instruction()` 分工明确——**一个管"说什么"（措辞/态度，给大模型），
  一个管"怎么念"（语速/气息，给 TTS）**。新增开关 `emotion.inject_to_context`（默认开）。
  现在情绪低落时，流萤的**话**也跟着收敛，不再出现"语气低沉但措辞活泼"的人格分裂
- ✅ **P1-5 记忆召回接入重要度/分类权重 + 重要记忆常驻**：此前 `search()` 只看关键词命中，
  记忆页里把某条调到重要度 10、归成「待办」对召回**零影响**（功能做了没接线）。现在：
  - 召回结果按 **`相关度(bm25) + 重要度×0.35 + 分类加分`** 综合重排，候选池放大 3 倍留给重排挑
  - 新增**常驻注入**：重要度 ≥ 门槛（默认 8）且未过期的记忆，**无论本轮话术是否命中都会带上**
    （默认最多 5 条），在召回块里以「【我一直记得的重要事情】」单列
  - 新增配置：`memory.pin_important` / `pin_min_importance` / `pin_limit`
- ✅ **D9 长问句召回兜底**：FTS trigram 是子串匹配，"豆豆怕什么来着？" 这种长问句几乎不可能
  整句命中（记忆里存的是"我的猫叫豆豆，很怕打雷"）。新增拆词兜底——整句没命中时，
  去停用词后抠出 2~3 字片段逐个 LIKE 取并集；核心词被吃成单字时（"我的猫怎么了"）再退回原句切片
- ✅ **P1-6 情绪预判提前**（治"慢一拍"）：新增 `EmotionModel.pre_turn_hint()`，在**生成回复之前**
  用关键词词典先垫一步（零成本、不调大模型），让**本轮**语气就跟上来；轮末仍照常跑完整推断精修。
  现在用户说"我今天特别累"，**第一句**回应就是低沉的，不用等下一句
- ✅ **P2-7 配置页补注入口**：新增「记忆与上下文注入」分组——每轮召回往事条数（1~15）、
  携带对话轮数（5~50）、重要记忆常驻开关 + 门槛 + 条数；「情绪模型」分组新增
  「把『此刻心情』注入大模型」开关。全部走 `_save_config` 的**逐字段校验**，填错只回退该项
- ✅ **P2-8 新增「🔍 本轮上下文」可视化**：对话页一键弹出**本轮实际发给大模型**的完整内容——
  按【人设】【动作说明】【往事召回】【此刻心情】【近 N 轮对话】分块显示各自字数，
  并给出模型/温度/估算 token 数、情绪与往事到底注没注入的明确标记。
  这是唯一能让用户**亲眼确认**"情感/记忆注没注入"的手段；情感页也新增了「将注入大模型上下文的心情」预览框
- ✅ 顺带对齐：`firefly_api.py` 的 `context` 接口同步补上心情段（和 `main.py` 的拼装规则保持一致，避免人格漂移）
- 🧪 自检 **31 项 → 36 项**（新增：情绪注入上下文、召回重排与常驻、长问句拆词、情绪预判提前、
  本轮上下文快照），36/36 全绿；另做了两轮隔离 GUI 冒烟（配置页新控件 + 情感页预览 + 弹窗）

### v0.5.1（桌宠随程序启动 + 声音设置对齐官方 + 模块联动 P0 修复）

- ✅ **桌宠随程序自动启动**：此前双击 exe 走图形控制台，而控制台代码从未拉起桌宠（只有语音模式会）。现控制台启动时按 `pet.enabled` 同步出现桌宠，`_quit_app` 联动关闭；`core/pet.py` 新增 `resolve_pet_options()`（缩放 0.5~2.5、不透明度 0.3~1.0 自动钳制）与 `stop_pet()` 队列退出命令，桌宠支持缩放与不透明度
- ✅ **桌宠设置并入控制台**：配置页新增「桌宠」分组（开关 / 演示 / 大小 / 不透明度 / 初始位置 +「重启桌宠」临时生效 +「预览四种状态」），独立桌宠设置页不再需要
- ✅ **声音设置全面修复（对齐小米 MiMo 官方文档）**：
  - 声克隆此前把参考音频放在顶层 `reference_audio` 字段（第三方代理写法），官方会忽略导致克隆失效；现按规范改为 `audio.voice = "data:{MIME};base64,..."`（MIME 仅 `audio/wav` / `audio/mpeg`，Base64 ≤ 10MB）
  - 音色设计模型（voicedesign）此前仍带预置 voice 参数，官方约定该模型**不传** voice，已改
  - 控制台「声音设置」分组：合成方式下拉 + 动态提示、预置音色下拉（mimo_default / 冰糖 / 茉莉 / 苏打 / 白桦 / Mia / Chloe / Milo / Dean）、音色描述、参考音频（浏览后自动校验 / 🎙 现场录制 5~30 秒）、🔊 试听、💾 单独保存声音设置；状态行绿/红反馈并说明原因
  - `core/tts.py` 抽出纯函数 `voice_choices` / `voice_display_for_id` / `voice_id_for_display` / `validate_reference_audio` / `tts_settings_issues`，界面与自检共用同一套校验
- ✅ **模块联动 P0 修复（本轮）**：
  - **自召回 bug**：`main.py` 的 `_prepare_messages` 先写库再召回，导致用户刚说的这句话必然命中自己（模型会看到"你刚提到…"的自我复读）。现改为**先召回、再入库**
  - **双情绪实例**：控制台情感页原来自建一份 `EmotionModel`，与对话用的那份互不相通——情感页微调不影响说话语气、对话演化也不更新数值条。现由 `ChatWorker` 只持有一份，注入给 `Firefly`（`emotion=` 参数 + `_owns_emotion` 生命周期管理），情感页通过后台快照读写，并**重读数据库**，两侧终于看的是同一个状态
  - **保存配置耦合**：原先「保存配置」把所有数字字段塞进一个 `try/except float()`，任一字段填错（如阈值手滑）会导致**整份配置都存不进去**（含音色）。现改为逐字段解析（`coerce_number` / `coerce_optional_int`），非法值单独回退到原值并**事后提示**哪几项没生效，其余照常保存
  - 附带修复：`reload`（保存配置后重载大脑）时旧 `Firefly` 的 SQLite 连接未关闭，反复保存会累积泄漏连接——现先 `close()` 再重建
- 🧪 自检 **29 项 → 31 项**（新增：情绪单实例注入「情感页↔对话联动」、保存配置字段解构「非法数字回退」），31/31 全绿；另加强了「全链路对话」用例，断言自召回行数应为 1

### v0.5.0（三入口合一 + 语音回复提速）

- ✅ **三入口整合成单 exe**：新增唯一入口 `firefly.py`，按参数分发四条分支——双击/无参数=图形控制台、`--pet`=桌宠、`--text`/`--selftest`/`--diag`/`--emotion`/`--stats` 等=对话与体检、`api <子命令>`=对外 JSON 接口。`build_exe.py` 由三 target 收敛为单 target，产物只剩一个 `流萤.exe`（体积从约 99 MB 降到约 35 MB）
- ✅ **黑窗口隐藏**：新增 `core/winconsole.py`，用 `GetConsoleProcessList` 判定「控制台是否自己独占」（避免把用户自己的 cmd 一起藏掉），独占才 `SW_HIDE`；`--text` 等需要控制台的模式自然保留
- ✅ **对外接口契约不变**：`流萤.exe api ping` 仍输出单行 JSON、退出码 0/1/2，可被外部程序用管道调用（自检项改为 `[sys.executable, "api", ...]`）
- ✅ **语音回复大幅提速（中度优化）**：
  - `core/http.py` 新增 `requests.Session` 单例 + 连接池，ASR / TTS / LLM 三处复用连接，省掉每轮三次 TLS 握手（约 100~300ms/次）
  - `core/llm.py` 新增 `chat_stream()`：`stream=True` 的 SSE 解析（connect/read 超时分离），流式失败自动回退非流式
  - `core/sentence_buffer.py` 分句器：按标点切句，**过滤 ACTION 行**（绝不念出 JSON）
  - `main.py` 新增 `_StreamSpeaker` 流式播报器：后台线程「按句合成 + 播放」带 1 句前瞻（等下一句到来才播当前句，规避带动作时的开场白先被念出），支持 barge-in 打断；`run_voice` 改走 `respond_stream()`
  - `tail_silence_seconds` 默认 1.0 → 0.5（说完停顿判结束更快，可再调）
- ✅ **情绪模型后台异步化**：`core/emotion.py` 连接改 `check_same_thread=False` + 加锁，新增 `update_from_turn_async()`；`respond()` 不再每轮同步等一次 LLM 往返，情绪在后台线程演化、下一轮生效
- 🧪 自检 **25 项 → 27 项**（新增：流式分句器切句+过滤 ACTION、LLM 流式 SSE 解析离线桩），27/27 全绿

### v0.4.0（记忆页 / 设置页完善 + 打包成可独立运行的 exe）

- ✅ **记忆页重做**（控制台）：Treeview 表格（时间 / 角色 / 分类 / 重要度 / 内容）、关键词搜索、按分类与最低重要度筛选、分页（每页 20/50/100/200）、选中条目完整内容面板、提高 / 降低重要度、加标签、删除（二次确认，连带清理 FTS 索引）、一键清理过期、导出 JSON / CSV（带 BOM，Excel 可直接打开）
- ✅ **设置页补齐**：新增「情绪模型」分组（启用 / 是否用大模型推断 / 风格模式 director·brief / 基准愉悦度·唤醒度·亲密度 / 每小时平复）与「Pi 协作」分组（启用 / 只读模式 / 超时 / 输出模式 / pi 路径 / 工作目录，带文件选择对话框）；保存即写回 `config.json`，且不改动其它既有配置
- ✅ `core/memory.py` 新增管理能力：`query` / `count_query` / `categories` / `delete`（同时清理 FTS 索引）/ `set_tags`
- ✅ **打包成可独立运行的 exe**：新增 `build_exe.py` 与 `打包exe.bat`，一次产出三个单文件 exe（`流萤助手.exe` / `流萤控制台.exe` / `流萤接口.exe`），并把 `persona\`、`config.example.json`、`使用说明.txt` 一并放到 `dist\`；自动用 pillow 画萤火虫图标，不引入额外二进制文件
- ✅ **打包适配（关键）**：`core/config.py` 新增 `is_frozen()` 与冻结模式根目录判定——打包后"项目根"指向 **exe 所在目录**而非临时解包目录，`config.json` / `data\` / `persona\` 都在 exe 旁边，整个文件夹拷走即用；`gui.py` 的配置保存、统计读取、体检子进程、开机自启脚本、文件选择框起始目录全部改走该根目录；`core/actions.py` 的受保护目录判断同步修正
- ✅ **首次运行更友好**：找不到 `config.json` 时自动按 `config.example.json` 生成一份并提示填 Key（此前会直接抛异常）
- ✅ 新增 `requirements.txt`；`.gitignore` 增加 `dist/`、`build/`、`*.spec`、`assets/*.ico`
- 🐞 **修复（严重）**：`gui.py` 从头到尾**没有调用 `mainloop()`**，导致控制台窗口"建好就退"——`控制台.bat` 与打包后的 `流萤控制台.exe` 都会一闪而过。现已补上事件循环（开发环境与打包环境均实测窗口正常常驻）
- 🔧 自检代码改为「冻结模式感知」：打包后不再 spawn `python firefly_api.py`（打包环境没有 python 与 .py 文件），改调同目录的 `流萤接口.exe`；也不再拿 `__file__` 推断项目根（打包后指向临时解包目录）——修复后**打包环境自检同样 25/25**
- 🔧 `firefly_api.py ping` 的 `llm_configured` 判定更准确（小米系地址也要真的有 Key 才算可用）
- 🧪 自检 **23 项 → 25 项**（新增：记忆管理筛选 / 翻页 / 删除、打包后项目根指向 exe 目录），25/25 全绿；GUI「记忆页 + 设置页」冒烟通过（含保存配置落盘校验、原有配置字段不丢失）

### v0.3.1（程序化情绪模型 + Pi 结果只存摘要）

- ✅ **程序化情绪模型** `core/emotion.py`（对齐小米 MiMo-V2.5-TTS 官方情绪方案）：
  - 三维数值情绪状态：愉悦度（-1~1）/ 唤醒度（0~1）/ 亲密度（0~1），持久化在 `data/memory.db`（`emotion_state` / `emotion_log`），重启不丢
  - 每轮由 MiMo 大模型推断情绪（严格 JSON 输出），**共情方向正确**：用户受挫时流萤跟着低落，不会反倒乐观；模型返回"安慰/建议"这类动作词会被过滤
  - 情绪平滑演化 + 随时间向基准自然平复（`decay_per_hour`），亲密度随互动缓慢增长
  - 推断失败（没配 Key / 网络异常）自动退化为关键词词典兜底，不影响正常对话
  - 输出符合官方规范的自然语言风格指令：`style_mode=director`（导演模式：角色 / 场景 / 指导）或 `brief`（一句话）；支持"温柔但疲惫"这类复合情绪
- ✅ **修复 TTS 缺口**：`core/tts.py` 原先只在 voicedesign 模型才发送 `role=user` 指令，导致默认的**预置音色模型根本无法表达情绪**。现按官方规范统一为「指令放 `role=user`、正文放 `role=assistant`」，预置音色 / voiceclone 也能接收风格指令（实测返回合法 wav）
- ✅ **Pi 结果只存摘要**：Pi 任务结果不再整段（原 2000 字截断）写入记忆，改为先用大脑压成不超过 240 字的中文摘要再入库；摘要失败则退化为截断，绝不丢结论（实测 1746 字 → 106 字）
- ✅ 控制台新增 **「情感」页**：三维进度条（愉悦度带零点中轴）、情绪变化曲线（愉悦度 / 唤醒度双线）、发给 TTS 的完整风格指令预览、手动微调（开心一点 / 安静一点 / 更亲近）与一键重置到基准；聊天回复后自动刷新
- ✅ 新增 `python main.py --emotion` 情绪体检（当前状态 + 将发给 TTS 的指令 + 最近变化）
- 🧪 自检 **19 项 → 23 项**（情绪演化与边界、情绪持久化、导演模式指令三段齐全、TTS 消息结构符合官方规范），23/23 全绿
- 🔒 自检保持离线：自检配置里关闭情绪的大模型推断（只走关键词），并把统计文件也指向临时目录

### v0.3.0（按需调用 Pi + 双扩展接口）

- ✅ **与 Pi 协作（可选，默认独立）**：陪伴端平时完全独立运行、不依赖 Pi；只有用户**明确要求**时才调用本机 `pi` 命令行。触发方式两种：对话里输入 `/pi <任务>`（键盘模式 / 控制台），或点名"用 Pi 帮我…"由模型按人设判断发出 `pi_agent` 动作
- ✅ **出站扩展点** `core/agent_backend.py`：`AgentBackend` 可插拔注册表（`register_backend` / `get_backend` / `list_backends`）+ `PiCliBackend`（自动探测 CLI、拼装参数、带超时的子进程执行、逐行流式回调、Windows `pi.cmd` 适配）
- ✅ **入站扩展点** `firefly_api.py`：给外部程序 / Pi 扩展调用的单行 JSON CLI（`ping` / `context` / `remember` / `say` / `search` / `persona` / `ask`），stdout 单行 JSON、日志走 stderr、退出码 0/1/2
- ✅ **安全**：`pi_agent` 登记为**硬闸口**（每次必须当面确认，自动流程绕不过），并写入审计日志；`config.json` 的 `pi.read_only=true` 可只授予读类工具（`read,grep,find,ls`）
- ✅ **记忆不共享**：长期记忆只在陪伴端；Pi 只是被调用方，其结果作为"笔记"记回流萤自己的记忆库
- ✅ **修复**：`core/actions.py` 的 `_protected()` 引用了未导入的 `PROTECTED_PATHS`，导致 `delete_file` / `delete_dir` **从未真正执行过**（自检第 11 项因"失败即拦截"而误判通过）——已修复并新增回归用例
- ✅ **解耦**：`core/config.py` 支持 `FIREFLY_ROOT` 环境变量覆盖项目根（不设时行为不变）；`core/memory.py` 启用 WAL + busy_timeout，支持多进程并发访问
- ✅ 新增 `python main.py --pi-check` 外部 agent 体检
- 🧪 自检 **14 项 → 19 项**，19/19 全绿；端到端实测（拒绝路径 / 只读任务）通过

### v0.2.0（桌宠 + 控制台 + 安全硬闸口）

- ✅ 桌宠「小萤火虫流萤」（`core/pet.py`，tkinter 零依赖）：待机呼吸 / 聆听波纹 / 思考转星 / 说话口型四态动画；可拖拽、贴边隐藏（鼠标移上恢复）、右键菜单、双击预览四态；随语音模式自动出现（`pet.enabled` 可关），也可 `python main.py --pet` 或 `桌宠.bat` 单独启动
- ✅ 图形控制台（`gui.py` + `控制台.bat`，tkinter 零依赖）四页：
  - 对话：文字聊天 + 麦克风说一句 + TTS 开关，与大模型调用全程后台线程不卡界面；危险操作弹窗确认（撤销清单）
  - 记忆：全量历史检索 / 最近对话浏览 / 记忆条数统计
  - 配置：API Key 掩码输入（不回显明文）、模型/音色/温度、唤醒词、录音阈值、语音打断与桌宠开关、开机自启（写入系统「启动」文件夹）、人设编辑器（保存即生效）
  - 状态：云端三关体检 / 离线自检一键运行、审计日志翻查、数据位置说明
- ✅ 安全框架 v2（F-06 闸口落地）：
  - 硬闸口：删除文件/目录、覆盖已有文件、外发类命令 → 即使自动流程也**必须当面确认**，代码级不可关闭；新增磁盘根目录/主目录/项目目录删除保护
  - 撤销清单（AC3）：大模型可一次提交多个 ACTION，执行前列出全部操作清单，一次确认或全部取消
  - 确认回调可插拔：命令行 input / GUI 弹窗由调用方注入，闸口规则不变
- ✅ 新增电脑操作：copy_file / move_file / delete_file（均需确认，删除为硬闸口）
- ✅ 声线定制落地（F-05）：人设每次对话重新加载（改文件即生效）；音色/模型等在控制台可视化修改，保存后自动重载大脑
- ✅ 环境修复：项目 venv 重建为带 tkinter 的 Python 3.13.14（旧 managed 版无 tkinter），依赖版本不变（numpy 2.5.3 / sounddevice 0.5.6 / requests 2.34.2）
- ✅ 自检扩充至 14 项：硬闸口判定与拒绝路径、批量 ACTION 解析、桌宠状态机、GUI 模块导入；14/14 全绿
- 🧪 冒烟测试：GUI 四页实例化+切换通过；桌宠四态各 30 帧绘制通过

### v0.1.0（MVP）

- ✅ 记忆系统：SQLite 本地持久化 + 中文关键词检索（FTS5 trigram）+ 相关往事自动召回
- ✅ 语音链路：唤醒（空格键兜底 + 可配 Picovoice「Hi Firefly」）→ MiMo-ASR → MiMo LLM → MiMo-TTS 播报
- ✅ 云端三关体检（`--diag`）与麦克风体检（`--mic-test`）
- ✅ 电脑操作：只读/低风险直接执行，写文件/命令强制二次确认 + 本地审计日志
- ✅ 云端兜底唤醒：识别到「Hi Firefly」即回应
- ✅ 语音打断（barge-in）：Firefly 说话时直接插话，它立刻闭嘴并听你说
- ✅ 蓝牙/声卡尾音截断修复（播放补静音垫底）
- ✅ 一键脚本：启动 / 服务体检 / 麦克风体检 / 键盘对话 / 检索记忆

---

## Git 提交记录

| 短哈希 | 提交时间 | 提交说明 | 完整哈希 |
| --- | --- | --- | --- |
| ee54c7c | 2026-09-07 02:40:49 | init: Firefly 语音助手 MVP 首个版本 | ee54c7c957702ccf83db7644cee115bfc4ba21cd |
| 1f35caa | 2026-09-07 02:41:26 | chore: 添加 .gitattributes 保护批处理文件不被换行转换 | 1f35caa82713ff0123eabccf0e347435b2ace8d1 |
| d993a1c | 2026-09-07 02:48:59 | feat: 新增语音打断(barge-in)，说话时可直接插话 | d993a1c7270dff04266b6e01a51c98ceddeee208 |
| 99fe74e | 2026-09-07 03:21:57 | feat: v0.2.0 桌宠+GUI控制台+安全硬闸口与撤销清单+声线配置化 | 99fe74e078ffccdfc39dbbd79ff81d08a315e662 |
| 6adde6f | 2026-09-07 03:22:37 | docs: README 自检项数 10→14 同步 | 6adde6fe01310118f3d97072d945eb3bd21c81b3 |
| 80e30f7 | 2026-09-07 20:42:45 | feat: v0.2.1 声线切换+操作增强+记忆增强+使用统计 | 80e30f74e8dec32ec9cd1e4f29de23b72a95c624 |
| 06790ab | 2026-09-07 21:06:47 | feat: v0.2.2 控制台增强 - 使用统计页+系统托盘 | 06790ab9a13d7589d828fc3e75236274ad8130bb |
| 6cc5642 | 2026-09-10 20:36:56 | fix: 修正 .gitattributes 规则顺序，恢复 bat 文件在仓库内的 CRLF 行尾 | 6cc5642f28f31906544f2a7937cb13ffa5b58822 |
| e9fe19f | 2026-09-10 22:26:01 | feat: v0.3.0 按需调用 Pi（/pi 命令 + 可插拔后端 + 对外 JSON 接口） | e9fe19f3469dbb0097edb2249b7a81259d0f04a5 |
| b12f1ef | 2026-09-10 22:48:42 | feat: v0.3.1 程序化情绪模型（对齐小米 MiMo 官方方案）+ Pi 结果只存摘要 | b12f1ef25020715669d3dad06bb43c1f24842697 |
| 951248b | 2026-09-10 23:12:31 | feat: v0.4.0 记忆页/设置页完善 + 打包成可独立运行的 exe | 951248b3b9b27f5db92161eb4b5999efbd56e02a |
| ccb3f1f | 2026-09-11 01:40:29 | fix: 修复打包后四个问题 | ccb3f1f736cf414c3a8a241cff619da12c25761f |
| d4b96f4 | 2026-09-12 00:56:55 | fix: 桌宠随控制台同步启动 + 桌宠设置整合进控制台（自检 28 项） | d4b96f4ba8de739dd48a541afaf358cb0415af45 |
| d1fcbbc | 2026-09-12 01:45:33 | fix: 声音设置全面修复（对齐 MiMo 官方文档）——克隆改走 audio.voice data URI + 音色下拉 + 确认按钮与反馈 | d1fcbbce299e7a5817762dce5bebf164e8b8af08 |
| e70bab7 | 2026-09-12 02:25:09 | fix: 模块联动 P0 三项修复（自召回 / 情绪单实例 / 保存配置解耦） | e70bab799f7cf3d962d3e967b2b1c4b4f7ae8efb |
| e87200c | 2026-09-12 02:25:38 | docs: 补充记忆架构方案、模块联动审查报告与技术研究报告 | e87200c0f46da9a40280b16be400840d5d1a3b98 |
| a66c371 | 2026-09-12 02:40:54 | feat: 模块联动 P1 + P2 落地（情绪注入上下文 / 召回重排与常驻 / 注入可视化） | a66c371b9d3d765abefc254be1289d18f0e6fd48 |
| 73cefff | 2026-09-12 03:09:56 | feat: 交互打磨 D3/D7 + 排查补齐确认与开关（v0.5.3） | 73cefffb1c31e295c853d345a48c967c3aec9754 |
| 534b1b9 | 2026-09-12 12:11:48 | feat(D1): 配置热重载 + GUI 可见性（v0.5.4） | 534b1b9bb95b8b9e123299d84a146576f2d51e85 |
| 366b567 | 2026-09-12 14:08:57 | fix: Pi 超时不再留孤儿进程 + 500 说人话（v0.5.5） | 366b5673e471c3a2b1ec1196a6c94fbb592c88fd |
| 4095aa9 | 2026-09-12 17:37:15 | docs: 新增交接说明（换人接手第一份要读的文档） | 4095aa97c34427c7a8a5243b7806fd1825a7b298 |
| 715567a | 2026-09-19 22:56:36 | 重构项目：重命名fairy为firefly，新增harness任务编排框架 | 715567a4e348c1ac651adda6c53c4cce18a2daaf |
| 3f4d0aa | 2026-09-20 16:10:34 | feat(控制台): 配置页增加滚动容器 + LLM/TTS/ASR 独立设置分组 | 3f4d0aa10207a0cf0362d830213555f0716c6431 |
