# 模块联动审查报告 · 声音 / 情感 / 记忆

审查时间：2026-09-12　审查范围：`core/tts.py`、`core/emotion.py`、`core/memory.py`、`core/llm.py`、`main.py`、`gui.py`
方法：逐环节读代码 + 追数据流（不做推测，每条结论都标注代码位置）

---

## 一、结论速览

| 需求 | 判定 | 一句话说明 |
|---|---|---|
| ① 配置里的音色能在对话播报中生效 | **基本实现，有 3 个缺口** | 控制台保存后会重载 Fairy → TTS 用新音色；但命令行模式必须重启，且保存会被无关字段连带阻断 |
| ② 情感设置全量注入上下文 | **未实现** | 情感**从不进入** LLM 上下文，只被翻译成 TTS 风格指令影响"怎么念"，文字内容完全感知不到情绪 |
| ③ 记忆全量注入上下文 | **未实现（属于设计取舍）** | 只注入「按当前话检索的 top-5」+「本会话最近 20 轮」；重要度/分类/标签对召回**零影响** |

---

## 二、需求①：音色能否在对话播报中实际生效

### 2.1 数据流（已实现的部分）

```
配置页「声音设置」→ voice_var（展示名）
   ↓ _current_tts_settings()：voice_id_for_display() 还原成音色 ID
config.json 的 tts.voice
   ↓ worker.submit("reload")  → gui.py:143-148  self.fairy = None → 重建 Fairy
Fairy.__init__  →  main.py:142  self.tts = make_tts(cfg)
   ↓ core/tts.py make_tts()：tts.voice 覆盖 mimo.voice（非空时生效）
MiMoTTS.synth()  →  payload["audio"]["voice"] = <配置音色>
   ↓ 播报链路
GUI: ChatWorker 第 166-169 行  f.say(reply) → main.py:415-444 say() → tts.synth()
```

**判定：这条链路是通的。** 在控制台里改音色 → 点「保存声音设置」或「保存配置」→ 重载后用新音色播报。`voice_instruction`（音色描述）与实时情绪风格也在同一处合并后发给 TTS（`main.py:156-172`）。

### 2.2 缺口

| 编号 | 问题 | 影响 | 位置 |
|---|---|---|---|
| D1 | 命令行模式（`--text` / 语音模式）启动时只读一次配置，改音色必须重启程序 | 用 `启动助手.bat` 的用户会以为"设置没生效" | `main.py:684-769`（run_app 只 load_config 一次） |
| D2 | 「保存配置」把音色和其它数字字段（静音阈值/温度/情绪基准/Pi 超时）绑在同一个 try 里，任一字段填错就**整份拒绝保存** | 用户改音色却因无关字段报错，音色也没存进去 | `gui.py:_save_config` 的 float 解析块 |
| D3 | 音色下拉只列 8 个预置音色；voiceclone 模式下音色框仍可编辑，容易误以为"克隆要选音色"（实际克隆音色来自参考音频） | 交互歧义 | `gui.py` 声音设置分组 |

---

## 三、需求②：情感与记忆是否全量注入上下文

### 3.1 记忆注入的实际情况

`main.py:182-196 _prepare_messages()` 每轮做两件事：

```python
recall  = self.memory.build_recall_block(user_text, top_k=cfg["memory"]["recall_top_k"])  # 默认 5 条
history = self.memory.recent(self.session_id, limit=cfg["llm"]["max_history_turns"])     # 默认 20 轮
```

`build_recall_block()`（`core/memory.py:173-183`）只取 `search()` 结果，而 `search()`（同文件 136-166）只按 **关键词/FTS 命中** 取前 N 条，**完全不看 `importance`、`category`、`tags`**。

**因此：**

- 注入的是 **选择性召回**，不是全量。这本身是合理的工程取舍（记忆库会无限增长，全量注入会爆上下文、降智）。
- 但**记忆页的管理动作与对话召回没有联动**：你把一条记忆的重要度调到 10、打上标签、归到「待办」，它在对话里的召回概率**一模一样**（只由文字命中决定）。这是"功能做了但没接上"的典型。
- 未命中关键词的长期记忆**永远不会**进入上下文（例如"我的猫叫豆豆"，用户说"我宠物怎么了"时可能召回不到）。
- 跨会话检索依赖 FTS trigram 命中，命中即无差别混入，不带分类/时间权重。

### 3.2 情感注入的实际情况：完全没有进上下文

全仓库扫描 `emotion` 的使用点只有三处：

| 位置 | 用途 |
|---|---|
| `main.py:156-172 _tts_instruction()` | 把情绪翻译成自然语言风格指令，**拼进 TTS 的 role=user** |
| `main.py:230-242 _record_turn()` | 一轮结束后**后台**更新情绪（`update_from_turn_async`） |
| `main.py:175-180 _system_prompt()` | ❌ **没有出现 emotion**（只有 persona + 动作说明 + recall） |

`_system_prompt()` 实际拼装：

```python
parts = [self.persona, "", actions.DESCRIPTIONS]
if recall: parts += ["【你记得的与当前话题相关的往事】", recall, ...]
```

**结论：情绪只影响"声音怎么念"，不影响"AI 说什么"。** 愉悦度/唤醒度/亲密度、情绪标签、主情绪原因（`reason`）这些语义信息，大模型一个字都看不到。表现为：情绪低落时，流萤的**语气**会是低落的，但**措辞**照样可能活泼——人格不一致。

### 3.3 额外发现的联动缺陷

| 编号 | 问题 | 影响 | 位置 |
|---|---|---|---|
| **D4** | **自召回**：`_prepare_messages` 先把当前这句 user 话写进记忆（184 行），再拿这句话去检索（188 行）→ 检索结果必然命中自己，当前话被重复塞进 recall 块 | 每轮上下文多一段重复噪音，压缩真实记忆的注入额度 | `main.py:184` vs `188` |
| **D5** | **GUI 有两个情绪实例**：情感页用 `self._emo`（`gui.py:689`），对话用 `ChatWorker.fairy.emotion`；两者只共享 SQLite 文件，**内存状态各算各的** | ① 情感页「开心一点/安静一点/重置」改的是 `_emo`，对话语气**不受影响**；② 对话演化写进了库，但情感页的**数值条不会更新**（`_refresh_emotion` 只调 `snapshot()` 读内存，从不 `load()`），只有下面的曲线因为直接查库会变——**页面自相矛盾** | `gui.py:689 / 763-782` |
| **D6** | 情绪更新是"回复播完之后"才做，且异步；本轮播报用的是**上一轮**的情绪 | 首轮情绪永远是基准值；用户突然说"我今天很难过"，这一句的回应语气不会跟着变（下一句才变） | `main.py:237-242` |
| **D7** | `emotion.enabled=false` 时，情感页只显示一句"已关闭"，但配置页与情感页之间没有"改完要重载"的联动提示 | 用户关掉情绪后不知道何时生效 | `gui.py:773-774` |
| **D8** | `memory.recall_top_k`、`llm.max_history_turns` 这两个**直接决定注入量**的参数在控制台里没有入口 | 用户无法调"记得多少/带多少上下文"，只能手改 config.json | 配置页缺字段 |

---

## 四、模块联动矩阵

| 上游模块 | 下游模块 | 是否已联动 | 证据 |
|---|---|---|---|
| 配置页 · 音色/合成方式/参考音频 | TTS 合成 | ✅ 已通（保存后重载） | `gui.py:_current_tts_settings` → `worker.submit("reload")` → `main.py:142` |
| 配置页 · 音色 | 命令行模式 | ⚠️ 需重启 | `main.py:run_app` 单次加载 |
| 配置页 · 情绪开关/基准/风格模式 | 情绪模型 | ✅ 已通（保存后重载 → 新 Fairy → 新 EmotionModel） | `gui.py` `_save_config` + reload |
| 配置页 · 桌宠 | 桌宠线程 | ✅ 已通（可即时重启） | `gui.py:_restart_pet` |
| 情感页 · 手动微调/重置 | 对话播报语气 | ❌ **不通**（双实例，D5） | `gui.py:784-794` |
| 情感模型 | LLM 上下文 | ❌ **不通**（从未注入） | `main.py:175-180` |
| 情感模型 | TTS 风格指令 | ✅ 已通（但慢一拍，D6） | `main.py:156-172` |
| 记忆库 · 写入 | 上下文（本会话近 20 轮） | ✅ 已通 | `main.py:192-195` |
| 记忆库 · 关键词召回 | 上下文 | ✅ 已通（含自召回噪音，D4） | `main.py:188` |
| 记忆页 · 重要度/分类/标签 | 上下文召回 | ❌ **不通**（检索不使用这些字段） | `core/memory.py:136-183` |
| 记忆页 · 删除/编辑 | 上下文 | ✅ 已通（同一 DB，下轮生效） | 同库直读 |
| Pi 协作结果 | 记忆库（category=笔记） | ⚠️ 写入了，但召回不按分类加权，易被普通闲聊淹没 | `main.py:359-360` |

---

## 五、后续开发方案

### P0（先修明确缺陷，改动小、收益直接）

1. **修 D4 自召回**：调整 `_prepare_messages` 顺序——先检索（用当前话做 query），再 `memory.add()`；或给 `build_recall_block` 传 `exclude_ids=[刚插入的 id]`。
   验收：自检新增一项断言"recall 块不包含本轮 user 原文"。
2. **修 D5 双情绪实例**：GUI 情感页不再自建 `EmotionModel`，改为通过 worker 转发两个动作——`emotion_snapshot`（优先用 `fairy.emotion.load()` 重读 DB）与 `emotion_nudge`（调 `fairy.emotion.nudge()`，让对话实例立刻生效）。
   验收：情感页点「😊 开心一点」后立刻试听，语气与数值同时变化；对话几轮后点刷新，数值条跟着涨。
3. **修 D2 保存耦合**：把配置页保存拆成"分组校验 + 分组保存"，或至少让数字字段错误只在对应分组内阻止保存。
   验收：故意把阈值填成 `abc`，仍能成功保存声音设置。

### P1（把"注入"做真，语义增强）

4. **情绪注入 LLM 上下文**（推荐做法，控制 token 开销）：
   在 `_system_prompt()` 中新增一段轻量状态行，而不是把三维数值原样丢进去：

   ```
   【此刻的心情】你现在是「温柔但有些疲惫」（愉悦度 +0.22 / 唤醒度 0.28 / 亲密度 0.41）。
   说话请与这份心情一致：措辞放软、语速慢，不要表现得亢奋。
   ```
   取 `describe()` + `_guidance()` 的产物，约 60~80 字；由 `emotion.enabled` 与新增开关 `emotion.inject_to_context`（默认 true）控制。
   验收：同一条用户输入，在 valence 明显为负时，回复措辞明显更收敛；自检加一项"system prompt 含情绪段"。
5. **记忆召回引入重要度/分类权重**：`search()` 的 FTS 结果按 `bm25 × importance 权重 + 分类加分` 重排；新增"常驻注入"——把 `importance >= 8` 且未过期的记忆（上限 5 条）无论是否命中都短列表附在 recall 块末尾。
   验收：把某条记忆重要度调到 10 后，即使本轮话术不命中关键词，它也会出现在注入块里。
6. **情绪更新提前**：`respond()` 里在生成回复**之前**做一次轻量关键词预判（`_lexicon_infer`，零成本，不调 LLM），让本轮语气先跟上来；完整 LLM 推断仍在轮末异步跑。
   验收：用户说"我今天特别累"的第一句回应，语气即为低沉。

### P2（可配置、可见）

7. **配置页补注入口**：新增「记忆注入」分组——`recall_top_k`（1~15）、`max_history_turns`（5~50）、`注入情绪到上下文` 开关、`重要记忆常驻` 开关。
8. **注入可视化（最推荐）**：对话页加一个「🔍 本轮上下文」按钮，弹出本次实际发给 LLM 的 system prompt + 消息条数 + token 估算。这是唯一能让用户**亲眼确认**"情感/记忆到底注没注入"的手段，也能把上面所有缺口变成可见事实。
   验收：点击后能分别看到【人设】【动作说明】【往事召回】【心情】【近 N 轮】五个区块及其字数。

---

## 六、如何自行验证（不依赖我）

| 想验证 | 操作 | 预期 |
|---|---|---|
| 音色是否生效 | 配置页选「苏打（中文）」→ 保存声音设置 → 对话页发一句并勾选播报 | 播报是男声 |
| 情绪是否影响文字 | 情感页点「🌙 安静一点」→ 对话页聊一句 | **当前版本：文字无变化（D5/D3.2 缺陷）** |
| 记忆是否命中 | 先说过"我的猫叫豆豆"，再问"豆豆怕什么" | 能召回（关键词命中）；换成"我宠物怎么了"则召回不到（3.1 缺口） |
| 重要度是否影响召回 | 记忆页把某条调到 10 → 用不命中的话去问 | **当前版本：无差别（D-3.1 缺陷）** |

---

## 七、修复进展（2026-09-12 更新）

以下是本报告结论落地后的实际状态，**代码级验证**（不是计划）：

| 编号 | 问题 | 状态 | 落地位置 / 验证 |
|---|---|---|---|
| D4 | 自召回 | ✅ 已修（P0） | `_prepare_messages` 改为「先召回、再入库」；自检断言自召回行数 == 1 |
| D5 | GUI 双情绪实例 | ✅ 已修（P0） | 情绪实例收敛到 `ChatWorker`，注入给 `Fairy`；自检 + 隔离 GUI 冒烟 |
| D2 | 保存配置耦合 | ✅ 已修（P0） | 逐字段 `coerce_number` / `coerce_optional_int`，事后非阻塞提示；自检 |
| D3.2 / P1-4 | 情绪从未进入 LLM 上下文 | ✅ 已修（P1） | `_system_prompt` 新增「此刻的心情」段（`EmotionModel.context_line()`），开关 `emotion.inject_to_context`；自检第 32 项 |
| 3.1 / P1-5 | 重要度/分类对召回零影响 | ✅ 已修（P1） | `search()` 按「bm25 + 重要度×0.35 + 分类加分」重排；新增「重要记忆常驻」；自检第 33 项 |
| D9 | 长自然语言问句召回率低 | ✅ 已修（P1） | 整句未命中时去停用词拆 2~3 字片段 LIKE 兜底；自检第 34 项 |
| D6 | 情绪慢一拍 | ✅ 已修（P1） | `pre_turn_hint()` 在回复生成前先垫一步；自检第 35 项 |
| D8 | 注入口无 GUI | ✅ 已修（P2） | 配置页「记忆与上下文注入」分组（recall_top_k / max_history_turns / 常驻开关与门槛）；隔离 GUI 冒烟 |
| — | 注入不可见 | ✅ 已修（P2） | 对话页「🔍 本轮上下文」分块展示 + token 估算；情感页新增心情预览框 |
| D1 | 命令行模式改音色须重启 | ✅ 已修 | 每轮对话前按 config.json 内容 sha1 探测，变了就地重建 TTS/ASR/LLM、重读人设与情绪设置（不重建情绪实例，亲密度保留）；`--text` 加 `/reload`；**GUI 侧 `on_config_reload` 回调 + `reload_note` 事件让状态栏 / 聊天区都能看到变更**；自检第 39 / 40 项 + GUI 冒烟 3 项 |
| D3 | voiceclone 下音色框歧义 | ✅ 已修 | `tts_field_states()` 按合成方式启用/禁用三个框并改标签（预置→音色可编辑；设计→音色禁用、必填音色描述；克隆→音色禁用、参考音频必填）；GUI 冒烟 3 项 |
| D7 | 情绪开关生效时机无提示 | ✅ 已修 | 配置页实时提示"点保存配置即刻生效"；情感页加状态行（已启用/已关闭）+「⚙ 去配置页」跳转；GUI 冒烟 4 项 |

### 7.1 顺手排查出的交互缺口（本轮一并修掉）

用户要求"自主决定还有哪些地方需要加确认按钮或者开关"，于是把控制台上所有可交互控件逐个过了一遍。
判据是**「这个操作会不会造成不可逆的损失」**——会，就必须确认；只是"改一下还能改回来"的，就不加（免得步步弹窗）。

| 控件 | 判断 | 处理 |
|---|---|---|
| 🧹 清理过期（记忆页 / 统计页） | **批量永久删除，原来一声不响就删了** | ✅ 先查数量 → 报"将删除 N 条" → 二次确认；无过期数据时只提示 |
| 🔁 重置到基准（情感页） | **连累积的亲密度一起重置，不可撤销** | ✅ 弹窗列出「当前值 → 基准值」三维对照再确认 |
| 保存配置（更换 API Key） | **旧 Key 从不回显，覆盖后找不回** | ✅ 换 Key 先确认；选「否」= 不换 Key、其余照常保存 |
| 退出程序（窗口/T 托盘） | **改了配置/人设没保存就退出 = 白改** | ✅ 检测未保存修改 → 拦截并列出具体字段 |
| 配置页底部 | 不知道"改了还没生效" | ✅ 新增「● 有 N 项修改未保存」提示 |
| 回复前关键词预判情绪 | **是我上一轮加的行为，却没给开关** | ✅ 新增 `emotion.pre_hint`（默认开），配置页可关 |
| 🔍 本轮上下文 | 反复点会开出一堆窗口 | ✅ 改为单例窗口 |
| ⭐ 提高 / 🔽 降低重要度 | 每次 ±1、可随时调回，不是不可逆 | ⬜ 保持无确认（加了反而烦） |
| 😊 / 🌙 / 💗 情绪微调 | 可反向调回，且"随手微调"就是要快 | ⬜ 保持无确认 |
| 🗑 删除选中 / 清空显示 / 保存人设 / 开机自启 | — | ✅ 本来就有确认，未改动 |
| 🔊 试听 / 🩺 体检 / 🧪 自检 / 导出 / 分页 / 筛选 / 桌宠设置 | 只读或可逆 | ⬜ 无需确认 |

> 另注：`main.py` 的 `--text` 与语音模式调用 `respond()` 时**不传 auto_confirm**，
> 也就是说低风险动作在真人使用路径上**从不自动放行**（auto_confirm 只给离线自检用），
> 安全姿态本来就是保守的，没有"该确认却没确认"的漏网之鱼。

> 自检项数：29 → 31（P0）→ 36（P1/P2）→ 39（D3/D7 + 交互确认 + D1 热重载）→ **40**（D1 可见性 print+回调），全绿。
> **本报告列出的缺口目前全部关闭**：P0 三项、P1 四项、P2 两项，以及 D1 / D3 / D7。
> 唯一保留的取舍是"记忆不做全量注入"（3.1 的结论）——那是刻意的工程选择，不是缺陷。
